# 🚀 Microservices Shipping System
### Powered by .NET 9, RabbitMQ, and Docker Swarm

هذا المشروع هو تطبيق عملي لنظام ميكروسيرفيسس (Microservices) متكامل، مصمم ليوضح كيفية بناء تطبيقات موزعة تتواصل عبر الرسائل (Asynchronous Messaging) ويتم إدارتها باستخدام بيئة Docker Swarm.

---

## 🏗️ بنية النظام (Architecture Hierarchy)

يعتمد النظام على أربعة مكونات أساسية تعمل في تناغم:

1.  **RabbitMQ (The Message Broker):** يمثل "البوسطجي" أو الوسيط. يقوم باستلام الرسائل من المرسل وتخزينها في طابور (Queue) لحين سحبها من قبل المستقبل.
2.  **Sender Service (.NET 9):** ميكروسيرفس تقوم بإنشاء طلبات شحن وهمية بشكل دوري وترسلها إلى RabbitMQ.
3.  **Receiver Service (.NET 9):** ميكروسيرفس (أو أكثر) تقوم بسحب الطلبات من RabbitMQ ومعالجتها.
4.  **Visualizer:** أداة رسومية لمراقبة توزيع الحاويات (Containers) على العقد (Nodes) داخل الـ Swarm.



---

## 🛠️ التقنيات المستخدمة (Tech Stack)

* **Framework:** .NET 9.0 (C#)
* **Messaging:** RabbitMQ (AMQP Protocol)
* **Orchestration:** Docker Swarm
* **Containerization:** Docker (Multi-stage Builds)
* **OS Environment:** WSL 2 (Windows Subsystem for Linux)

---

## 🚀 خطوات التشغيل (Deployment Guide)

اتبع هذه الخطوات بدقة لتشغيل النظام بالكامل على جهازك:

### 1. بناء صور الدوكر (Docker Images)
بما أن الكود تم تطويره على .NET 9، نقوم ببناء الصور داخل بيئة الدوكر لضمان التوافق:

```bash
# بناء صورة خدمة الإرسال
docker build -t my-sender ./SenderApp
. تفعيل Docker Swarm

تأكد من تفعيل وضع السويرم في جهازك:
Bash

docker swarm init

3. إطلاق المنظومة (Stack Deploy)

باستخدام ملف docker-compose.yml الموحد، نقوم بتشغيل جميع الخدمات:
Bash

docker stack deploy -c docker-compose.yml shipping-system

📊 أدوات الإدارة والمراقبة

بمجرد التشغيل، يمكنك مراقبة النظام عبر الروابط التالية:

    RabbitMQ Management Portal: * الرابط: http://localhost:15672

        المستخدم: guest | كلمة السر: guest

        راقب تبويب الـ Queues لمشاهدة تدفق الرسائل.

    Swarm Visualizer:

        الرابط: http://localhost:8080

        يعرض لك عدد الحاويات الشغالة وتوزيعها.

⚙️ شرح الأكواد والعمليات التقنية
🐳 الـ Dockerfiles (التحسين)

تم استخدام Multi-stage Build لتقليل حجم الصور النهائية:

    Stage 1 (Build): تحميل الـ SDK 9.0 وعمل dotnet publish.

    Stage 2 (Runtime): نسخ الملفات الناتجة فقط إلى صورة runtime خفيفة جداً، مما يجعل الحاوية سريعة في التشغيل والتحميل.

🔄 التواصل بين الخدمات

يتم الربط بين الخدمات باستخدام اسم الخدمة في الدوكر (hostname: rabbitmq) بدلاً من الـ IP، مما يسهل عملية الـ Scalability.
📈 التوسع الديناميكي (Scaling)

يمكنك زيادة عدد "المستقبلين" في أي وقت لمواجهة ضغط الرسائل عبر الأمر:
Bash

docker service scale shipping-system_receiver-service=5

📁 هيكلية المشروع
Plaintext

.
├── SenderApp/
│   ├── Program.cs        # منطق إرسال الرسائل
│   └── Dockerfile        # مراحل بناء خدمة الإرسال
├── ReceiverApp/
│   ├── Program.cs        # منطق استقبال ومعالجة الرسائل
│   └── Dockerfile        # مراحل بناء خدمة الاستقبال
├── docker-compose.yml    # المانيفستو الخاص بتعريف الـ Stack
└── README.md             # هذا الملف التعليمي

تم تطوير هذا المشروع كجزء من تجربة بناء الأنظمة الموزعة عالية الأداء.
# بناء صورة خدمة الاستقبال
docker build -t my-receiver ./ReceiverApp
